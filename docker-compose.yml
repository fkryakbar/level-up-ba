services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/fkryakbar/level-up-ba:latest
    container_name: level-up-ba
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local